# Memory

Use this directory for durable learning:

- `regressions.md`: escaped failures converted into scenario requirements.
- `calibration.md`: human-labeled examples and judge mistakes.
- `golden.md`: excellent traces that future judges should preserve.
- `bad.md`: unsafe or low-quality traces that future judges should catch.

The first implementation keeps generated trace JSON under `qa-loop/traces/`.
